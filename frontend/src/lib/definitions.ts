export type Transaction = {
	id: string;
	title: string;
	amount: number;
	category: string;
	description: string;
	date: string;
};

export type FormField = {
	title: string;
	amount: number;
	category: string;
	description: string;
	date: string;
};
